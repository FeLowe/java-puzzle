# Puzzle - Plain English Specs:

1. It takes a word from the user and returns a string:
Input: cat
output: "cat"

2. It takes the inputted word and returns a string where vowel is replaced by a dash symbol:
Input: cat
output: "c-t"
